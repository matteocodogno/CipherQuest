spring:
  application:
    name: CipherQuest

  threads:
    virtual:
      enabled: true

application:
  name: CipherQuest
  version: '@project.version@'


info:
  app:
    name: '@project.name@'
    description: '@project.description@'
    version: '@project.version@'
